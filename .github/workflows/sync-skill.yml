name: Sync SKILL.md

on:
  push:
    branches: [main]
    paths:
      - skills/soulmate/SKILL.md

concurrency:
  group: sync-skill
  cancel-in-progress: false

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main

      - name: Pull latest before making changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git pull --rebase origin main

      - name: Copy to duplicate locations
        run: |
          cp skills/soulmate/SKILL.md soulmate/SKILL.md
          cp skills/soulmate/SKILL.md plugins/soulmate/skills/soulmate/SKILL.md

      - name: Rebuild soulmate.skill ZIP
        run: |
          cd skills && zip -r ../soulmate.skill soulmate/

      - name: Commit and push if changed
        run: |
          git diff --quiet && exit 0
          git add soulmate/SKILL.md plugins/soulmate/skills/soulmate/SKILL.md soulmate.skill
          git commit -m "chore: sync SKILL.md copies and soulmate.skill [skip ci]"
          git push
